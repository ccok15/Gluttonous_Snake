# DEVLOG

## 2026-03-21

- 调整本局规则与展示：`移动加速` 的抽取权重改回普通权重；`暴击豆` 不再被 `微型磁吸` 自动吸走；`穿墙术` 更名为 `穿越术`，现在无论撞墙还是撞到自身都可消耗 `1` 次机会通过。
- 玩家可见影响：只要仍持有穿越机会，蛇头会切换为特殊颜色；主 HUD 与蛇头附近不再显示具体剩余次数；`穿越术` 升级卡只显示“增加 1 次穿越机会”。

## 2026-03-18

- 初始化 Godot 4.6.1 贪吃蛇 x 肉鸽项目骨架。
- 玩家可见影响：项目现在有可直接打开的主入口、主菜单、游戏场景、升级弹窗、HUD 与调试 HUD。
- 回滚提示：删除新增的 `scenes/`、`scripts/`、`data/`、`assets/` 与 `project.godot` 即可回到空仓库。

## 2026-03-18

- 接入轻量本地化框架，当前默认语言改为简体中文。
- 玩家可见影响：主菜单、升级弹窗、HUD、调试 HUD 与升级名称/描述改为中文显示；后续可通过新增语言资源扩展多语言版本。
- 回滚提示：删除 `LanguagePack`、相关本地化资源与 UI 文案键调用即可回退到单语言硬编码版本。

## 2026-03-18

- 新增 `docs/planning/` 资料区，并按 `design/`、`balance/`、`spreadsheets/` 分类整理策划与数值文件；现有升级整理表已迁入该目录。
- 玩家可见影响：项目里的策划文档、数值文档和表格资料现在有固定存放位置，后续查找和维护会更清晰。
- 回滚提示：把 `docs/planning/` 删除，并将需要保留的资料文件移回原位置即可。

## 2026-03-19

- 按最新升级表重组升级池：移除 `路径预览`、`阶段缓冲`、`转向缓存`，新增 `突进`、`暴击豆`、`穿墙术`，并同步更新 HUD、调试 HUD、中文文案与升级资源。
- 玩家可见影响：双击同方向可触发突进，真豆现在可按升级随机获得更高分数，撞墙时可消耗穿墙次数从地图另一侧穿出，且剩余穿墙次数会显示在蛇头附近。
- 回滚提示：还原 `data/config/upgrades/`、`default_run_config.tres`、`game_run.gd`、HUD 相关脚本与升级整理表即可。

## 2026-03-19

- 按 `run_balance_config.xlsx` 同步整局节奏配置：开局改为 `20x20` 小地图、`1` 颗豆、`60` 秒阶段时长，并把阶段地图尺寸、豆子数量和升级经验曲线接入资源配置与运行逻辑。
- 玩家可见影响：游戏前期节奏变快，阶段推进时地图会逐步变大、豆子会逐步变多，调试 HUD 也会显示当前棋盘尺寸。
- 回滚提示：还原 `run_config.gd`、`stage_config.gd`、`game_run.gd`、`data/config/run/`、`data/config/stages/` 与 `run_balance_config.xlsx` 对应同步结果即可。

## 2026-03-19

- 放大棋盘单格显示尺寸，并在死亡后新增可点击的“重新开始”按钮；同时把总控表中的 `cell_size_px` 同步到最新值。
- 玩家可见影响：当前每个格子的视觉面积更大，前期小地图更容易看清；本局失败后可以直接点按钮重开，不必只靠键盘热键。
- 回滚提示：还原 `GameHud.tscn`、`game_hud.gd`、`game_run.gd`、本地化资源、`default_run_config.tres` 与总控 Excel 中的 `cell_size_px` 即可。

## 2026-03-19

- 调整 `移动加速` 升级逻辑，改为“基于初始移动速度的累计加速”，不再把每级写成固定最终速度；同步更新升级资源、中文文案与升级整理表。
- 玩家可见影响：后续如果调整初始移动速度，`移动加速` 会自动按新的基础速度计算，不需要再单独重填每级目标速度。
- 回滚提示：还原 `move_speed_up.tres`、`game_run.gd`、本地化资源与升级整理表中的 `移动加速` 行即可。

## 2026-03-19

- 重做升级经验曲线：从 `49` 经验之后继续按“每级新增经验再多 `3` 点”的规律增长，并把 20 级以后的经验外推逻辑同步改为延续该规律，不再固定封顶到 `30`。
- 玩家可见影响：中后期升级节奏会更平滑，20 级以后所需经验也会继续递增，不会突然回到旧的固定增长规则。
- 回滚提示：还原 `run_balance_config.xlsx`、`run_config.gd` 与 `default_run_config.tres` 中的经验阈值和溢出规则即可。

## 2026-03-19

- 将整条升级经验曲线的“每次升级新增经验”整体下调 `2` 点，并同步到总控 Excel 与默认运行配置。
- 玩家可见影响：整局升级速度会更快，尤其是前中期更容易连上升级节奏，但后续仍保持逐级递增的压力。
- 回滚提示：还原 `run_balance_config.xlsx`、`run_config.gd` 与 `default_run_config.tres` 中的经验阈值数组即可。

## 2026-03-19

- 先行同步下一版玩法规则到策划文档与表格：移动速度改快、阶段改为通关加时、地图改成每两阶段扩图、普通豆数量大幅上调，并把暴击豆拆成“下限 / 上限 / 留存”三条升级分支。
- 玩家可见影响：这次改动目前只进入文档与表格，还没有进代码；后续实现将以这些文档为准。
- 回滚提示：还原 `docs/planning/spreadsheets/` 与 `docs/planning/design/run_rules_v2.md` 的本次修改即可。

## 2026-03-19

- 把新版节奏和特殊豆规则正式接入游戏：初始速度提到 `90 ms/步`、`移动加速` 改为每级累计 `-15 ms` 且权重更高、阶段改为通关后追加 `60` 秒、地图每两阶段扩到下一档、普通豆从首关 `4` 颗开始按阶段 `+4` 增长，并新增 `P` 暂停与吃豆飘字。
- 玩家可见影响：本局开场更快、更密集，特殊暴击豆会以独立颜色和闪烁方式出现，得分区间会随“下限 / 上限 / 留存”三条升级分别成长；失败后仍可直接点击按钮或按 `R` 重开。
- 回滚提示：还原 `game_run.gd`、`bean_tile_map.gd`、`score_popup_layer.gd`、`GameRun.tscn`、`GameHud.tscn`、`game_hud.gd`、`default_run_config.tres`、`data/config/stages/`、`data/config/upgrades/` 与本地化资源即可。

## 2026-03-19

- 从升级池中移除 `护撞充能`，并同步删掉运行时护撞计数逻辑、资源引用、中文文案和升级整理表。
- 玩家可见影响：升级三选一里不再出现“护撞充能”；当前局内唯一的碰撞保护只剩升级后自带的 `0.5` 秒保护期。
- 回滚提示：恢复 `crash_guard.tres`、`default_run_config.tres`、`game_run.gd`、本地化资源和升级整理表中的该条目即可。
